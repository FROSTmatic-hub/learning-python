# What are functions in Python?
- Functions in python are built-in features that python provides in order to perform an action. 
- Functions can be called directly:
```python
a = 2
print(a)
#print() is a function
```
- Other examples of functions include:
	- `len()`
	- `print()`
	- `int()`
	- etc. 