# What is String Immutability?
- Immutability means that a value cannot be changed once it is created
- It can only be reassigned
- Strings are immutable and their values can only be reassigned but cannot be changed using indexes. 