# What is the `type()` function?
- `type()` is used to tell the type of the data value.
```python
print(type(2)) #will print int

print(type(2.4)) #will print float

print(type("Hello")) #will print str
```
